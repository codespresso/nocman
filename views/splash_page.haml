!!! 5
%html

  %head
    %meta(charset="utf-8")
    %title Welcome to Nocman

    %link(href="/static/css/bootstrap.css", rel="stylesheet")
    %link(href="/static/css/screenly.css", rel="stylesheet")

  %body.splash

    .container
      .row
        .span12
          .logo-container.page-header.text-center
            %img(src="/static/img/nocman_large.png")

      .row
        .span12.text-center
          - if ip_lookup:
            %p.lead To manage the content on this screen, use the following IP

            .well.lead
              ${screen_ip}
          - else:
            %p.lead Unable to resolve management IP.

      %hr